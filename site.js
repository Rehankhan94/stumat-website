const menuButton=document.querySelector('.menu-button');
const navigation=document.querySelector('.nav');
if(menuButton&&navigation){menuButton.addEventListener('click',()=>{const open=navigation.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open));});navigation.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{navigation.classList.remove('open');menuButton.setAttribute('aria-expanded','false');}));}
if('IntersectionObserver' in window && !window.matchMedia('(prefers-reduced-motion: reduce)').matches){document.documentElement.classList.add('can-reveal');const observer=new IntersectionObserver(entries=>{for(const entry of entries){if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target);}}},{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));}else{document.querySelectorAll('.reveal').forEach(el=>el.classList.add('visible'));}
const motionToggle=document.querySelector('.motion-toggle');
if(motionToggle){const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const setPaused=paused=>{document.documentElement.classList.toggle('motion-paused',paused);motionToggle.setAttribute('aria-pressed',String(paused));motionToggle.setAttribute('aria-label',paused?'Play scientific animations':'Pause scientific animations');motionToggle.innerHTML=paused?'Play animation <span aria-hidden="true">▶</span>':'Pause animation <span aria-hidden="true">Ⅱ</span>';};setPaused(reduced);motionToggle.addEventListener('click',()=>setPaused(!document.documentElement.classList.contains('motion-paused')));}
const environmentRange=document.querySelector('#environment-range');
if(environmentRange){const label=document.querySelector('#environment-label');environmentRange.addEventListener('input',()=>{const amount=Number(environmentRange.value);const hue=Math.round(194+amount*.9);document.querySelector('.indicator-film').style.setProperty('--indicator-color',`hsl(${hue} 42% 47%)`);label.textContent=amount<20?'Initial state':amount<80?'Changing conditions':'Changed conditions';});}
